#!/usr/bin/env bash
# Thin shim. The orchestrator is stdlib-only Python in src/punch/.
set -euo pipefail

repo_root="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONPATH="${repo_root}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m punch "$@"
