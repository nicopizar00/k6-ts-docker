# CLAUDE.md（範本 · Claude Code 轉接輻）

> ⚠ TEMPLATE. Adapt before use. `.github/` = single source of truth.

Claude Code 於此倉，奉 `.github/` 為治理正源（hub）。本檔但轉接，不另立規。

## 序（read order）

1. `.github/copilot-instructions.md`（中樞戒律）
2. `.github/instructions/**`（路徑細則）
3. `.github/prompts/**`（生命週期 prompt）

## 戒

- 不分叉、不覆、不另定 `.github/` 之規 — 唯復用。
- 生命週期、mode 紀律同中樞。
- 改未驗不為成。

## ⚠ 警（caveman ultra）

> ⚠ Claude Code = secondary host. Primary = GitHub Copilot VS Code.
> ⚠ No fork `.github/`. Reuse only.
